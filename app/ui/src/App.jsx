import React from 'react';

export default function App() {
  return (
    <div style={{ padding: '40px', textAlign: 'center' }}>
      <h1 className="gradient-text" style={{ fontSize: '2.5rem', marginBottom: '16px' }}>CampusSync Web Dashboard</h1>
      <p style={{ color: 'var(--text-secondary)', fontSize: '1.1rem' }}>
        Frontend starter ready. We will build out the React UI components in the next step!
      </p>
    </div>
  );
}
